{
  "hash": "8993ee67b342b5b75b4def06a128d4d2",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What airspeed does it trim at when ballasted to the required CG?\"\n---\n\n\n::: {#bebc6d5e .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 90.0   # s for this render, granted by the user\nSOLVE_BUDGET = 15.0     # s for any one solve\nPROBE_POOL = 200.0        # s granted for the probe\nPROBE_SPENT = 26.9      # s the probe actually used\n\nmass, cg = get_mass_properties(get_airplane())\nmac = get_airplane().c_ref\n\nop1 = asb.OperatingPoint(velocity=10, alpha=0)\naero1 = asb.AeroBuildup(airplane=get_airplane(), op_point=op1).run()\nop2 = asb.OperatingPoint(velocity=10, alpha=1)\naero2 = asb.AeroBuildup(airplane=get_airplane(), op_point=op2).run()\n\nCL_alpha = (aero2[\"CL\"] - aero1[\"CL\"]) / np.radians(1)\nCm_alpha = (aero2[\"Cm\"] - aero1[\"Cm\"]) / np.radians(1)\n\nx_np = get_airplane().xyz_ref[0] - mac * Cm_alpha / CL_alpha\nreq_cg = float(np.array(x_np - 0.1 * mac).flatten()[0])\n\nop_trim = asb.OperatingPoint(velocity=10, alpha=0)\naero_trim = asb.AeroBuildup(airplane=get_airplane(), op_point=op_trim, xyz_ref=[req_cg, 0, get_airplane().xyz_ref[2]]).run()\ncm_0 = float(np.array(aero_trim[\"Cm\"]).flatten()[0])\n```\n:::\n\n\n::: {.hero}\nNone\n[Cannot trim upright]{.hero-label}\n:::\n\n**Answer.** It cannot trim at any airspeed. With a symmetric flat-plate wing and no incidence, the pitching moment at zero lift is [\\-0\\.043]{.key}. Combined with a stable 10% static margin, the aircraft requires a negative lift coefficient to achieve zero pitching moment, meaning it cannot produce the upward lift required to balance its weight in level flight.\n\n::: {#cell-fig-pitching-moment .cell execution_count=3}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\n\nalphas = np.linspace(-5, 10, 16)\ncms = []\ncls = []\nfor a in alphas:\n    op = asb.OperatingPoint(velocity=10, alpha=a)\n    aero = asb.AeroBuildup(airplane=get_airplane(), op_point=op, xyz_ref=[req_cg, 0, get_airplane().xyz_ref[2]]).run()\n    cms.append(float(np.array(aero[\"Cm\"]).flatten()[0]))\n    cls.append(float(np.array(aero[\"CL\"]).flatten()[0]))\n\nfig, ax = plt.subplots(figsize=(6, 4))\nax.plot(cls, cms, 'k.-')\nax.axhline(0, color='gray', linestyle='--')\nax.axvline(0, color='gray', linestyle='--')\nax.set_xlabel(\"Lift coefficient $C_L$\")\nax.set_ylabel(\"Pitching moment $C_m$\")\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Pitching moment versus lift coefficient at the required CG. The aircraft crosses zero pitching moment only at negative lift, precluding upright trim.](2026-09-24-02-what-airspeed-does-it-trim-at-when-ballasted-to-the-required-cg_files/figure-html/fig-pitching-moment-output-1.png){#fig-pitching-moment width=888 height=589}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef get_mass_properties(plane, area_density=0.1744, foam_thickness=0.005):\n    vol_density = area_density / foam_thickness\n    total_mass = 0\n    total_moment = np.zeros(3)\n    \n    for wing in plane.wings:\n        area_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(wing.xsecs)-1):\n            x0 = wing.xsecs[i]\n            x1 = wing.xsecs[i+1]\n            \n            dy = x1.xyz_le[1] - x0.xyz_le[1]\n            dz = x1.xyz_le[2] - x0.xyz_le[2]\n            ds = np.sqrt(dy**2 + dz**2)\n            \n            c0, c1 = x0.chord, x1.chord\n            dx_le = x1.xyz_le[0] - x0.xyz_le[0]\n            dc = c1 - c0\n            \n            strips = 50\n            x_cg_sec = 0\n            area_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                chord_t = c0 + t*dc\n                x_le_t = x0.xyz_le[0] + t*dx_le\n                strip_area = chord_t * ds * dt\n                strip_x_cg = x_le_t + chord_t / 2\n                x_cg_sec += strip_x_cg * strip_area\n                area_sec += strip_area\n                \n            cg = np.array([x_cg_sec/area_sec, 0, 0])\n            cg_tot += cg * area_sec\n            area_tot += area_sec\n            \n        cg = cg_tot / area_tot\n        m = area_tot * area_density\n        total_mass += m\n        total_moment += m * cg\n\n    for fuse in plane.fuselages:\n        vol_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(fuse.xsecs)-1):\n            x0 = fuse.xsecs[i]\n            x1 = fuse.xsecs[i+1]\n            \n            dx = x1.xyz_c[0] - x0.xyz_c[0]\n            dy = x1.xyz_c[1] - x0.xyz_c[1]\n            dz = x1.xyz_c[2] - x0.xyz_c[2]\n            ds = np.sqrt(dx**2 + dy**2 + dz**2)\n            \n            a0, a1 = x0.xsec_area(), x1.xsec_area()\n            da = a1 - a0\n            \n            strips = 50\n            x_cg_sec = 0\n            vol_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                area_t = a0 + t*da\n                x_c_t = x0.xyz_c[0] + t*dx\n                \n                strip_vol = area_t * ds * dt\n                x_cg_sec += x_c_t * strip_vol\n                vol_sec += strip_vol\n                \n            cg = np.array([x_cg_sec/vol_sec, 0, 0]) if vol_sec > 0 else np.zeros(3)\n            cg_tot += cg * vol_sec\n            vol_tot += vol_sec\n            \n        cg = cg_tot / vol_tot if vol_tot > 0 else np.zeros(3)\n        m = vol_tot * vol_density\n        total_mass += m\n        total_moment += m * cg\n        \n    return total_mass, total_moment / total_mass\n\n```\n:::\n[Rendered in 1.6 s (limit 90 s) · explored in 27 s (limit 200 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-24-02-what-airspeed-does-it-trim-at-when-ballasted-to-the-required-cg_files"
    ],
    "filters": [],
    "includes": {}
  }
}