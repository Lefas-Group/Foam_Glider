{
  "hash": "4d7c825945bd139908fe47d12b7498a7",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"How long does it stay airborne from a standing throw?\"\n---\n\n\n::: {#f14d7add .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 90.0   # s for this render, granted by the user\nSOLVE_BUDGET = 15.0     # s for any one solve\nPROBE_POOL = 200.0        # s granted for the probe\nPROBE_SPENT = 141.5      # s the probe actually used\n\nimport aerosandbox as asb\nimport aerosandbox.numpy as np\n\nplane = get_airplane()\nmass, cg = get_mass_properties(plane)\nreq_cg_x = get_cg_x(plane)\n\nincidences = np.linspace(-30, 0, 15)\ntimes = []\nfor inc in incidences:\n    p2 = apply_incidence(plane, inc)\n    alpha_trim = get_trim_alpha(p2, req_cg_x, cg[2], bracket=[-5, 40])\n    if np.isnan(alpha_trim):\n        times.append(0.0)\n        continue\n    v_trim, ld, sink = get_trimmed_flight_state(p2, mass, req_cg_x, cg[2], alpha_trim)\n    if not np.isnan(sink) and sink > 0:\n        times.append(1.5 / sink)\n    else:\n        times.append(0.0)\n\nbest_trimmed_time = max(times)\nbest_inc = incidences[np.argmax(times)]\n\n# Untrimmed maximum time\nopt = asb.Opti()\nv = opt.variable(init_guess=4.0, lower_bound=1.0, upper_bound=20.0)\nalpha = opt.variable(init_guess=5.0, lower_bound=-10.0, upper_bound=20.0)\n\naero = asb.AeroBuildup(\n    airplane=plane,\n    op_point=asb.OperatingPoint(velocity=v, alpha=alpha),\n).run()\n\nL = aero[\"L\"]\nD = aero[\"D\"]\ngamma = np.arctan(D / L)\n\nopt.subject_to(L * np.cos(gamma) + D * np.sin(gamma) == mass * 9.81)\n\nsink_rate = v * np.sin(gamma)\nopt.minimize(sink_rate)\nsol = opt.solve(verbose=False)\n\nuntrimmed_time = 1.5 / float(sol.value(sink_rate))\n```\n:::\n\n\n::: {.hero}\n[1\\.20 s]{.hero-value}\n[Best trimmed flight time from 1.5 m]{.hero-label}\n:::\n\n**Answer.** From a standing throw of 1.5 m, the aircraft stays airborne for at most [1\\.20]{.key} seconds. Trimming at a required 10% static margin demands significant negative incidence ([\\-19\\.3°]{.key}), and the associated trim drag severely degrades endurance. If it could fly at its theoretical minimum sink rate without trim considerations, it would achieve up to [1\\.81]{.key} s. Moving the wings rearward slightly improves both times over the [original configuration](../01-first-chapter/2026-09-25-01-does-it-meet-the-2-s-flight-time-target.qmd).\n\n::: {#cell-fig-flight-time-rear .cell execution_count=3}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\n\nfig, ax = plt.subplots(figsize=(6, 4))\nax.plot(incidences, times, '-k', label='Trimmed Flight Time')\nax.axhline(untrimmed_time, color='r', linestyle='--', label=f'Untrimmed Max ({untrimmed_time:.2f} s)')\n\nax.set_xlabel(\"Wing Incidence (°)\")\nax.set_ylabel(\"Flight Time (s)\")\nax.set_ylim(0, 2.5)\nax.grid(True)\nax.legend()\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Trimmed flight time from 1.5 m against wing incidence. High negative incidence is required to balance the aircraft, severely impacting endurance compared to its theoretical untrimmed potential.](2026-09-25-03-how-long-does-it-stay-airborne-from-a-standing-throw_files/figure-html/fig-flight-time-rear-output-1.png){#fig-flight-time-rear width=889 height=590}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef get_mass_properties(plane, area_density=0.1744, foam_thickness=0.005):\n    vol_density = area_density / foam_thickness\n    total_mass = 0\n    total_moment = np.zeros(3)\n    \n    for wing in plane.wings:\n        area_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(wing.xsecs)-1):\n            x0 = wing.xsecs[i]\n            x1 = wing.xsecs[i+1]\n            \n            dy = x1.xyz_le[1] - x0.xyz_le[1]\n            dz = x1.xyz_le[2] - x0.xyz_le[2]\n            ds = np.sqrt(dy**2 + dz**2)\n            \n            c0, c1 = x0.chord, x1.chord\n            dx_le = x1.xyz_le[0] - x0.xyz_le[0]\n            dc = c1 - c0\n            \n            strips = 50\n            x_cg_sec = 0\n            area_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                chord_t = c0 + t*dc\n                x_le_t = x0.xyz_le[0] + t*dx_le\n                strip_area = chord_t * ds * dt\n                strip_x_cg = x_le_t + chord_t / 2\n                x_cg_sec += strip_x_cg * strip_area\n                area_sec += strip_area\n                \n            cg = np.array([x_cg_sec/area_sec, 0, 0])\n            cg_tot += cg * area_sec\n            area_tot += area_sec\n            \n        cg = cg_tot / area_tot\n        m = area_tot * area_density\n        total_mass += m\n        total_moment += m * cg\n\n    for fuse in plane.fuselages:\n        vol_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(fuse.xsecs)-1):\n            x0 = fuse.xsecs[i]\n            x1 = fuse.xsecs[i+1]\n            \n            dx = x1.xyz_c[0] - x0.xyz_c[0]\n            dy = x1.xyz_c[1] - x0.xyz_c[1]\n            dz = x1.xyz_c[2] - x0.xyz_c[2]\n            ds = np.sqrt(dx**2 + dy**2 + dz**2)\n            \n            a0, a1 = x0.xsec_area(), x1.xsec_area()\n            da = a1 - a0\n            \n            strips = 50\n            x_cg_sec = 0\n            vol_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                area_t = a0 + t*da\n                x_c_t = x0.xyz_c[0] + t*dx\n                \n                strip_vol = area_t * ds * dt\n                x_cg_sec += x_c_t * strip_vol\n                vol_sec += strip_vol\n                \n            cg = np.array([x_cg_sec/vol_sec, 0, 0]) if vol_sec > 0 else np.zeros(3)\n            cg_tot += cg * vol_sec\n            vol_tot += vol_sec\n            \n        cg = cg_tot / vol_tot if vol_tot > 0 else np.zeros(3)\n        m = vol_tot * vol_density\n        total_mass += m\n        total_moment += m * cg\n        \n    return total_mass, total_moment / total_mass\n\ndef get_cg_x(airplane):\n    op1 = asb.OperatingPoint(velocity=10, alpha=0)\n    aero1 = asb.AeroBuildup(airplane=airplane, op_point=op1).run()\n    op2 = asb.OperatingPoint(velocity=10, alpha=1)\n    aero2 = asb.AeroBuildup(airplane=airplane, op_point=op2).run()\n    \n    CL_alpha = (aero2[\"CL\"] - aero1[\"CL\"]) / np.radians(1)\n    Cm_alpha = (aero2[\"Cm\"] - aero1[\"Cm\"]) / np.radians(1)\n    mac = airplane.c_ref\n    x_np = airplane.xyz_ref[0] - mac * Cm_alpha / CL_alpha\n    return float(np.array(x_np - 0.1 * mac).flatten()[0])\n\ndef get_trim_alpha(airplane, req_cg_x, cg_z, bracket=[-10, 80]):\n    def get_Cm(alpha):\n        res = asb.AeroBuildup(\n            airplane=airplane,\n            op_point=asb.OperatingPoint(velocity=5.0, alpha=alpha),\n            xyz_ref=[req_cg_x, 0, cg_z]\n        ).run()['Cm']\n        return float(np.atleast_1d(res)[0])\n    try:\n        return scipy.optimize.root_scalar(get_Cm, bracket=bracket).root\n    except Exception:\n        return np.nan\n\ndef get_trimmed_flight_state(airplane, mass, req_cg_x, cg_z, alpha_trim):\n    mg = mass * 9.81\n    res_trim = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(velocity=5.0, alpha=alpha_trim),\n        xyz_ref=[req_cg_x, 0, cg_z]\n    ).run()\n    L_5 = float(np.atleast_1d(res_trim['L'])[0])\n    \n    if L_5 <= 0:\n        return np.nan, np.nan, np.nan\n        \n    v_trim = 5.0 * np.sqrt(mg / L_5)\n    \n    res_exact = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(velocity=v_trim, alpha=alpha_trim),\n        xyz_ref=[req_cg_x, 0, cg_z]\n    ).run()\n    L = float(np.atleast_1d(res_exact['L'])[0])\n    D = float(np.atleast_1d(res_exact['D'])[0])\n    \n    gamma = np.arctan2(D, L)\n    sink = v_trim * np.sin(gamma)\n    \n    return float(v_trim), float(L/D), float(sink)\n\ndef apply_incidence(airplane, inc):\n    p2 = airplane.copy()\n    for w in p2.wings:\n        for xsec in w.xsecs:\n            xsec.twist = inc\n    return p2\n\n```\n:::\n[Rendered in 18.9 s (limit 90 s) · 1 aero solve (budget 15 s each) · explored in 142 s (limit 200 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-25-03-how-long-does-it-stay-airborne-from-a-standing-throw_files"
    ],
    "filters": [],
    "includes": {}
  }
}