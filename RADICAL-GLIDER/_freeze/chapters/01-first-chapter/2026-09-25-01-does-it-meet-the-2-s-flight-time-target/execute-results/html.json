{
  "hash": "db6e204b595873b2d7bac6c851ead9da",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Does it meet the 2 s flight-time target?\"\n---\n\n\n::: {#02eb5171 .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 90.0\nSOLVE_BUDGET = 15.0\nPROBE_POOL = 200.0\nPROBE_SPENT = 118.8\n\nimport aerosandbox as asb\nimport aerosandbox.numpy as np\n\nplane = get_airplane()\nmass, cg = get_mass_properties(plane)\nrequired_cg = get_cg_x(plane)\nballast_mass = mass * (cg[0] - required_cg) / (required_cg - 0)\ntotal_mass = mass + ballast_mass\nmg = total_mass * 9.81\n\n# 1. Untrimmed performance (Theoretical Maximum Flight Time)\nopt = asb.Opti()\nv = opt.variable(init_guess=4.0, lower_bound=1.0, upper_bound=20.0)\nalpha = opt.variable(init_guess=5.0, lower_bound=-10.0, upper_bound=20.0)\n\naero_untrimmed = asb.AeroBuildup(\n    airplane=plane,\n    op_point=asb.OperatingPoint(velocity=v, alpha=alpha),\n).run()\n\nL = aero_untrimmed[\"L\"]\nD = aero_untrimmed[\"D\"]\ngamma = np.arctan(D / L)\n\nopt.subject_to(L * np.cos(gamma) + D * np.sin(gamma) == mg)\n\nsink_rate = v * np.sin(gamma)\nopt.minimize(sink_rate)\nsol = opt.solve(verbose=False)\n\nuntrimmed_sink = float(sol.value(sink_rate))\nuntrimmed_time = 1.5 / untrimmed_sink\n\n# 2. Trimmed performance\nplane_with_cg = asb.Airplane(\n    name=plane.name,\n    xyz_ref=[required_cg, 0, plane.xyz_ref[2]],\n    wings=plane.wings,\n    fuselages=plane.fuselages\n)\n\nincidences = np.linspace(-30, -5, 26)\ntrimmed_times = []\n\nfor inc in incidences:\n    p2 = apply_incidence(plane_with_cg, inc)\n            \n    alpha_trim = get_trim_alpha(p2, bracket=[-10, 80])\n    \n    if np.isnan(alpha_trim):\n        trimmed_times.append(0.0)\n        continue\n        \n    v_trim, ld_trim, sink_trim = get_trimmed_flight_state(p2, total_mass, alpha_trim)\n    if np.isnan(sink_trim):\n        trimmed_times.append(0.0)\n    else:\n        trimmed_times.append(1.5 / sink_trim)\n\nbest_trimmed_time = np.max(trimmed_times)\nbest_inc = incidences[np.argmax(trimmed_times)]\n```\n:::\n\n\n::: {.hero}\n[No\\, 0\\.79 s]{.hero-value}\n[Best trimmed flight time from 1.5 m]{.hero-label}\n:::\n\n**Answer.** The aircraft cannot physically stay airborne for 2.0 s from a 1.5 m throw. Even flying untrimmed at its absolute maximum endurance, the theoretical best flight time is only [1\\.77]{.key} s. \n\nTrimming it makes the sink rate significantly worse. The required nose-up pitching moment can only be achieved by raising the wing trailing edge to negative incidences. The longest possible trimmed flight time occurs at [\\-21\\.0]{.key}° of incidence, yielding [0\\.79]{.key} s. The fuselage drag at this high trim angle ruins the lift-to-drag ratio.\n\n::: {.callout-tip}\n## New user specifications\nAsked of the user, 2026-09-25:\n\n1. **flight-time target: 2 s** — Determines if requirements are met\n:::\n\n::: {#cell-fig-trimmed-time .cell execution_count=3}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\n\nfig, ax = plt.subplots(figsize=(6, 4))\nax.plot(incidences, trimmed_times, '-k', label='Trimmed Flight Time')\nax.axhline(untrimmed_time, color='r', linestyle='--', label=f'Untrimmed Max ({untrimmed_time:.2f} s)')\nax.axhline(2.0, color='g', linestyle='-.', label='Target (2.0 s)')\n\nax.set_xlabel(\"Wing Incidence (°)\")\nax.set_ylabel(\"Flight Time (s)\")\nax.set_ylim(0, 2.5)\nax.grid(True)\nax.legend()\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Trimmed flight time from 1.5 m against wing incidence. Flying slower to reduce sink rate requires steep negative incidence to counter the fuselage's pitch-down moment, but extreme angles cause immense trim drag.](2026-09-25-01-does-it-meet-the-2-s-flight-time-target_files/figure-html/fig-trimmed-time-output-1.png){#fig-trimmed-time width=889 height=590}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef get_mass_properties(plane, area_density=0.1744, foam_thickness=0.005):\n    vol_density = area_density / foam_thickness\n    total_mass = 0\n    total_moment = np.zeros(3)\n    \n    for wing in plane.wings:\n        area_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(wing.xsecs)-1):\n            x0 = wing.xsecs[i]\n            x1 = wing.xsecs[i+1]\n            \n            dy = x1.xyz_le[1] - x0.xyz_le[1]\n            dz = x1.xyz_le[2] - x0.xyz_le[2]\n            ds = np.sqrt(dy**2 + dz**2)\n            \n            c0, c1 = x0.chord, x1.chord\n            dx_le = x1.xyz_le[0] - x0.xyz_le[0]\n            dc = c1 - c0\n            \n            strips = 50\n            x_cg_sec = 0\n            area_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                chord_t = c0 + t*dc\n                x_le_t = x0.xyz_le[0] + t*dx_le\n                strip_area = chord_t * ds * dt\n                strip_x_cg = x_le_t + chord_t / 2\n                x_cg_sec += strip_x_cg * strip_area\n                area_sec += strip_area\n                \n            cg = np.array([x_cg_sec/area_sec, 0, 0])\n            cg_tot += cg * area_sec\n            area_tot += area_sec\n            \n        cg = cg_tot / area_tot\n        m = area_tot * area_density\n        total_mass += m\n        total_moment += m * cg\n\n    for fuse in plane.fuselages:\n        vol_tot = 0\n        cg_tot = np.zeros(3)\n        for i in range(len(fuse.xsecs)-1):\n            x0 = fuse.xsecs[i]\n            x1 = fuse.xsecs[i+1]\n            \n            dx = x1.xyz_c[0] - x0.xyz_c[0]\n            dy = x1.xyz_c[1] - x0.xyz_c[1]\n            dz = x1.xyz_c[2] - x0.xyz_c[2]\n            ds = np.sqrt(dx**2 + dy**2 + dz**2)\n            \n            a0, a1 = x0.xsec_area(), x1.xsec_area()\n            da = a1 - a0\n            \n            strips = 50\n            x_cg_sec = 0\n            vol_sec = 0\n            for t in np.linspace(0, 1, strips):\n                dt = 1/strips\n                area_t = a0 + t*da\n                x_c_t = x0.xyz_c[0] + t*dx\n                \n                strip_vol = area_t * ds * dt\n                x_cg_sec += x_c_t * strip_vol\n                vol_sec += strip_vol\n                \n            cg = np.array([x_cg_sec/vol_sec, 0, 0]) if vol_sec > 0 else np.zeros(3)\n            cg_tot += cg * vol_sec\n            vol_tot += vol_sec\n            \n        cg = cg_tot / vol_tot if vol_tot > 0 else np.zeros(3)\n        m = vol_tot * vol_density\n        total_mass += m\n        total_moment += m * cg\n        \n    return total_mass, total_moment / total_mass\n\ndef get_cg_x(airplane):\n    op_point = asb.OperatingPoint(velocity=10, alpha=0)\n    aero_stab = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=op_point\n    ).run_with_stability_derivatives()\n    x_np = aero_stab['x_np'][0]\n    mac = airplane.wings[0].mean_aerodynamic_chord()\n    return float(x_np - 0.10 * mac)\n\ndef get_trim_alpha(airplane, bracket=[-10, 80]):\n    def get_Cm(alpha):\n        res = asb.AeroBuildup(\n            airplane=airplane,\n            op_point=asb.OperatingPoint(velocity=5.0, alpha=alpha),\n        ).run()['Cm']\n        return float(np.atleast_1d(res)[0])\n    try:\n        return scipy.optimize.root_scalar(get_Cm, bracket=bracket).root\n    except Exception:\n        return np.nan\n\ndef get_trimmed_flight_state(airplane, mass, alpha_trim):\n    mg = mass * 9.81\n    res_trim = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(velocity=5.0, alpha=alpha_trim),\n    ).run()\n    L_5 = float(np.atleast_1d(res_trim['L'])[0])\n    \n    if L_5 <= 0:\n        return np.nan, np.nan, np.nan\n        \n    v_trim = 5.0 * np.sqrt(mg / L_5)\n    \n    res_exact = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(velocity=v_trim, alpha=alpha_trim),\n    ).run()\n    L = float(np.atleast_1d(res_exact['L'])[0])\n    D = float(np.atleast_1d(res_exact['D'])[0])\n    \n    gamma = np.arctan2(D, L)\n    sink = v_trim * np.sin(gamma)\n    \n    return float(v_trim), float(L/D), float(sink)\n\ndef apply_incidence(airplane, inc):\n    p2 = airplane.copy()\n    for w in p2.wings:\n        for xsec in w.xsecs:\n            xsec.twist = inc\n    return p2\n\n```\n:::\n[Rendered in 30.9 s (limit 90 s) · 1 aero solve (budget 15 s each) · explored in 119 s (limit 200 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-25-01-does-it-meet-the-2-s-flight-time-target_files"
    ],
    "filters": [],
    "includes": {}
  }
}