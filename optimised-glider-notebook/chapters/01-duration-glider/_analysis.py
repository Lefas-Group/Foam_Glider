# =============================================================================
# How this chapter measures the duration glider.
#
# Empty by design. Helpers arrive here by promotion -- when a SECOND entry
# reaches for something an earlier one wrote inline -- never by anticipation.
# Anything describing the aircraft rather than a measurement of it belongs in
# _model.py; anything about the notebook itself belongs in _notebook.py.
#
# _scratch/probe.py prints api() on every run, so what is already here is
# visible at the moment someone is about to write a helper.
# =============================================================================
