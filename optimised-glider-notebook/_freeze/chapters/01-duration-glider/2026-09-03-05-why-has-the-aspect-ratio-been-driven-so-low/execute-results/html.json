{
  "hash": "5af27093f35c8c8996b86910389d688c",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Why has the aspect ratio been driven so low?\"\n---\n\n\n::: {#7357fc73 .cell execution_count=2}\n``` {.python .cell-code}\n# Aspect ratio held at each point with the other three variables re-optimised\n# around it, so a swept design is never judged against rivals that were not\n# allowed to adapt to it.\nARS = [2.5, 3.0, 3.5, 4.0, 5.0, 6.0, 8.0, 10.0, 12.0]\nsweep = [optimise(static_margin=0.10, hold={\"aspect_ratio\": ar}) for ar in ARS]\n\nlo, hi = sweep[0], sweep[-1]\nbest = min(sweep, key=lambda r: r[\"sink\"])\nprof_share = best[\"CDp\"] / (best[\"CDp\"] + best[\"CDi\"])\nflat = [r for r in sweep if r[\"sink\"] < 1.02 * best[\"sink\"]]\n\nassert best[\"CDp\"] > best[\"CDi\"]  # the section, not the span, is the cost\nassert hi[\"CDp\"] > lo[\"CDp\"] and hi[\"CDi\"] < lo[\"CDi\"]  # the two terms oppose\nassert hi[\"mass\"] < lo[\"mass\"]  # low AR is the HEAVIER choice, and still wins\n```\n:::\n\n\n::: {.hero}\n[65%]{.hero-value}\n[of the drag at the optimum is the section, not the wingspan]{.hero-label}\n:::\n\n**Answer.** Span is fixed, so chord is the only lever on thickness ratio, and\n5 mm stock on a 25 mm chord is a\n[20%]{.key} slab. Across the sweep profile drag rises\n3\\.9× while induced drag falls\n8× (@fig-ar, @tbl-ar). Low aspect ratio is\nalso the heavier choice, by [3\\.3×]{.key},\nand still wins.\n\n::: {.callout-warning}\n## A fact about the foam, not about gliders\nThinner stock would move this more than any reshaping.\n:::\n\n::: {.callout-note}\n## Assumed\n\n1. **Static margin 10%** at every swept point.\n2. **One start per solve**; the surface has several basins.\n3. **Aspect ratio confined to \\(2\\.5\\, 12\\.0\\).**\n:::\n\n::: {#cell-fig-ar .cell execution_count=3}\n``` {.python .cell-code}\nfig, (ax1, ax2) = plt.subplots(1, 2, figsize=(7.0, 2.7))\n\nax1.plot(ARS, [r[\"CDp\"] for r in sweep], \"o-\", ms=4, label=\"profile\")\nax1.plot(ARS, [r[\"CDi\"] for r in sweep], \"s-\", ms=4, label=\"induced\")\nax1.set_ylabel(\"drag coefficient\")\nax1.legend()\n\nax2.plot(ARS, [r[\"sink\"] for r in sweep], \"o-\", ms=4, color=\"C2\")\nax2.axvspan(min(r[\"aspect_ratio\"] for r in flat),\n            max(r[\"aspect_ratio\"] for r in flat), color=\"C0\", alpha=0.12)\nax2.plot(best[\"aspect_ratio\"], best[\"sink\"], \"o\", ms=8, color=\"C0\")\nax2.set_ylabel(\"sink rate, m/s\")\n\nfor ax in (ax1, ax2):\n    ax.set_xscale(\"log\")\n    ax.set_xticks(ARS, [f\"{a:g}\" for a in ARS])\n    ax.minorticks_off()\n    ax.set_xlabel(\"aspect ratio\")\n    ax.grid()\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Left: the two drag components against aspect ratio, each at its own re-optimised design. Right: the resulting sink rate, with the swept minimum marked and the band within 2% of it shaded. Aspect ratio is on a log scale.](2026-09-03-05-why-has-the-aspect-ratio-been-driven-so-low_files/figure-html/fig-ar-output-1.png){#fig-ar width=1038 height=395}\n:::\n:::\n\n\n::: {#tbl-ar .cell tbl-cap='The swept designs. Chord, thickness ratio and Reynolds number are consequences of aspect ratio at fixed span; mass, the two drag components and sink are results of the solve at each point.' execution_count=4}\n| AR | chord, mm | t/c | Re | mass, g | CD profile | CD induced | sink, m/s |\n|---:|---:|---:|---:|---:|---:|---:|---:|\n| 2.5 | 120 | 4.2% | 26406 | 11.52 | 0.0522 | 0.0364 | 0.582 |\n| 3.0 | 100 | 5.0% | 21360 | 9.89 | 0.0573 | 0.0365 | 0.547 |\n| 3.5 | 86 | 5.8% | 18304 | 8.60 | 0.0612 | 0.0324 | 0.538 |\n| 4.0 | 75 | 6.7% | 16264 | 7.37 | 0.0636 | 0.0257 | 0.548 |\n| 5.0 | 60 | 8.3% | 13787 | 5.66 | 0.0675 | 0.0150 | 0.629 |\n| 6.0 | 50 | 10.0% | 11911 | 5.89 | 0.1003 | 0.0168 | 0.795 |\n| 8.0 | 38 | 13.3% | 10695 | 4.44 | 0.1009 | 0.0060 | 1.241 |\n| 10.0 | 30 | 16.7% | 7898 | 3.75 | 0.1728 | 0.0069 | 1.554 |\n| 12.0 | 25 | 20.0% | 7257 | 3.51 | 0.2031 | 0.0044 | 2.143 |\n:::\n\n\n[Executed in 46.8 s · 18 aero solves]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-03-05-why-has-the-aspect-ratio-been-driven-so-low_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}