---
title: "<the question, exactly as the user asked it>"
---

{{< include _model.qmd >}}

<!-- If the work has moved past this entry, say so here, above everything:
```{python}
#| echo: false
#| output: asis
superseded_by("<successor stem>", "<one short sentence on what changed>")
```
-->

```{python}
<Compute what was asked for and no more. Bind results to names; do not print
 them -- the hero, the answer and the figure below pull from these.
 Reach for the chapter's shared machinery rather than restating it;
 _scratch/probe.py prints api() on every run, so what exists is already on
 screen. A claim the prose makes but does not quote gets an `assert`, so the
 page fails to render rather than going quietly stale.>
```

<!-- One hero, or none. .hero-pair when the answer IS a comparison; delete the
     block when the answer is a figure, a yes/no, or a value nobody asked for. -->
::: {.hero}
[`{python} f"<the one value this entry exists to produce>"`]{.hero-value}
[<one line: what it means. Not what to conclude from it.>]{.hero-label}
:::

**Answer.** <The answer, in prose. Supporting values get [`{python} …`]{.key}.
Cross-reference the visual as (@fig-<name>). If the question also asks HOW --
build steps, a procedure -- it continues here as a numbered list, never as a
second `**Assembly.**` section.>

::: {.callout-tip}
## Specified
<Asked of the user, <date>:>

1. **<quantity>: <value>** — <why, if it fits>.
:::

::: {.callout-note}
## Assumed
1. **<quantity>: <value>**, <the one-clause reason>.
:::

::: {.callout-warning}
## <The caveat, as a title>
<Only when the answer comes with a real health warning -- "do not trust that
number", "what this cannot measure".>
:::

<!-- ONE visual, or none. Prefer none, then a table, then a plot: a figure costs
     far more to read than a small table, so plot only when the SHAPE is the
     argument. A table uses `#| label: tbl-<name>` and `#| tbl-cap:` instead. -->
```{python}
#| label: fig-<name>
#| fig-cap: "<what is plotted. Not what to conclude from it.>"
<plotting code — plain matplotlib, legends rather than direct labels; pass an
 explicit figsize, since draw_three_view() and friends ignore the rcParams>
```

```{python}
#| echo: false
#| output: asis
footer(<the shared functions this entry called -- from _analysis.py. footer()
        renders them, then what the entry cost to run. Bare footer() ONLY when
        the entry called nothing from _analysis.py; rule 13 checks this, against
        inline expressions as well as cells. Pass what the entry NAMES, not what
        those functions call in turn.>)
```

<!--
Notes on this template — delete this comment in real entries.

The shape above is the instruction, and SKILL.md's "Entry format" carries the
rules. One thing neither shows:

FRONT MATTER IS THE TITLE ONLY. No `date`, `categories` or `description` -- they
render as a "Published" block, tag chips and a subtitle. Filenames carry the
date and the sidebar sorts on filename, so ordering is unaffected either way.

Then run `uv run python .claude/skills/design-notebook/lint.py <notebook>`; its
messages name the fix. Render traps live in `references/quarto.md`.
-->
