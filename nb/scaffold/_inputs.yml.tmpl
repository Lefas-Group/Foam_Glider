# What this chapter NEWLY committed to and NEWLY assumed. Rendered into its
# index.qmd by `chapter_inputs()` -- do NOT write these callouts by hand, or
# every item appears on the page twice.
#
# `- <id>: <text>` — the id is a handle, not prose, and need only be unique
# within this file. A later chapter that REPLACES one of these names it in its
# own _fork.yml under `supersedes: - NN-this-chapter: <id>`, which moves the
# item into a Superseded callout here. Rule 8: ten words per item.
#
# The test for what belongs here: IS IT TRUE OF EVERY ENTRY THIS CHAPTER WILL
# HAVE? A number one question produced belongs in that entry. Delete a block
# with nothing in it -- an empty callout is never rendered.
specified:
assumed:
