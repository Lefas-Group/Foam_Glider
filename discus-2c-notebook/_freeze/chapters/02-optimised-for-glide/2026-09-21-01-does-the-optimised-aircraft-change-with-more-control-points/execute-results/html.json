{
  "hash": "01b3816f1b90d12b185fcf7b0c6302d4",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Does the optimised aircraft change if there are more control points to play with?\"\n---\n\n\n::: {#5bda842f .cell execution_count=2}\n``` {.python .cell-code code-summary=\"Show code\"}\nSUBDIVISIONS = [1, 2, 3, 4]\n\nsetup = baseline_setup()\ndesigns = [constrained_redesign(setup, subdivisions=n) for n in SUBDIVISIONS]\nstation_counts = [d[\"stations\"] for d in designs]\n\nverdicts = method_verdicts(designs, setup)\n\ncoarse, fine = designs[0], designs[-1]\non_bound = sum(1 for c in fine[\"chords\"] if c > CHORD_MAX - 1e-6)\nspan_settled = abs(designs[-1][\"span\"] - designs[-2][\"span\"]) / designs[-2][\"span\"] * 100\nspread = [a - b for a, b in zip(verdicts[\"AeroBuildup\"], verdicts[\"LiftingLine\"])]\n\n# The finding is that refinement widens the gap between the methods while the\n# geometry stops moving. Both halves are asserted: if a future change made the\n# shape keep drifting, or the gap stop widening, this entry would be describing\n# an experiment it no longer ran.\nassert spread[-1] > spread[0], \"refinement did not widen the disagreement\"\nassert span_settled < 1.0, \"span has not settled by the finest grid\"\n```\n:::\n\n\n::: {.hero-pair}\n::: {}\n[\\-3\\.9%]{.hero-value}\n[LiftingLine's verdict, four stations]{.hero-label}\n:::\n::: {}\n[\\-18\\.5%]{.hero-value .hero-ref}\n[the same, at thirteen]{.hero-label}\n:::\n:::\n\n**Answer.** Yes, and worse. Refining four stations to\n13 moves span\n[19\\.91]{.key} →\n[21\\.98]{.key} m and raises the optimiser's claimed\ngain to [10\\.7]{.key}%, while\nLiftingLine's verdict falls to\n[\\-18\\.5]{.key}% (@fig-stations). The\nshape converges — span moves 0\\.03% over the last\nrefinement — while the disagreement doubles. Freedom buys exploitation,\nnot aircraft.\n\n::: {.callout-tip}\n## Specified\nAsked of the user, 2026-09-21:\n\n1. **Refinement study**: more control points, up to thirteen.\n:::\n\n::: {.callout-warning}\n## The finest designs rest on a bound\n\nAt thirteen stations 4 inboard chords sit on their\nupper limit, so refinement partly buys more places to press against a\nbuildability bound that was chosen, not derived.\n:::\n\n::: {#cell-fig-stations .cell execution_count=3}\n``` {.python .cell-code}\nfig, (ax_trend, ax_chord) = plt.subplots(1, 2, figsize=(7.0, 3.0))\n\nfor (label, colour) in zip([\"AeroBuildup\", \"LiftingLine\"], [\"C0\", \"C1\"]):\n    ax_trend.plot(station_counts, verdicts[label], \"-o\", ms=5,\n                  color=colour, label=label, zorder=3)\nax_trend.axhline(0, color=\"C2\", lw=0.8, zorder=2)\nax_trend.set_xticks(station_counts)\nax_trend.set_xlabel(\"Station count\")\nax_trend.set_ylabel(\"Change in best glide ratio [%]\")\nax_trend.grid(alpha=0.3)\nax_trend.legend(loc=\"center right\", fontsize=8)\n\nfor design, label, colour in [(coarse, f\"{coarse['stations']} stations\", \"C2\"),\n                              (fine, f\"{fine['stations']} stations\", \"C0\")]:\n    ax_chord.plot(design[\"fractions\"] * design[\"span\"] / 2, design[\"chords\"],\n                  \"-o\", ms=4, color=colour, label=label, zorder=3)\nax_chord.axhline(CHORD_MAX, color=\"C1\", lw=1.4, ls=\"--\", zorder=4,\n                 label=\"buildability bound\")\nax_chord.set_xlabel(\"Distance from centreline [m]\")\nax_chord.set_ylabel(\"Chord [m]\")\nax_chord.set_ylim(0, CHORD_MAX * 1.15)\nax_chord.grid(alpha=0.3)\nax_chord.legend(loc=\"center right\", fontsize=7)\n\nplt.tight_layout()\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Left: change in best glide ratio against the baseline, by station count, judged by each method on its own terms. Right: the chord distribution at the coarsest and finest grids, with the buildability bound the inboard stations run into.](2026-09-21-01-does-the-optimised-aircraft-change-with-more-control-points_files/figure-html/fig-stations-output-1.png){#fig-stations width=1039 height=440}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef baseline_setup():\n    \"\"\"\n    The baseline aircraft, as everything an optimization entry needs to start from.\n\n    Promoted when a second entry rebuilt the same four lines: the calibrated\n    polar, its best-glide point, the drag offset fitted there, and the limits the\n    optimizer inherits. Bundled rather than left loose so that two entries cannot\n    optimize against subtly different baselines and then compare their answers.\n\n    Costs one `reference_point` plus one derivative call, about seven solves.\n    \"\"\"\n    reference = reference_point()\n    offset = reference[\"polar\"][\"drag_offset\"]\n    return {\n        \"drag_offset\": offset,\n        \"speed\": reference[\"speed\"],\n        \"L_over_D\": reference[\"L_over_D\"],\n        \"limits\": baseline_limits(offset),\n    }\n\ndef constrained_redesign(setup=None, subdivisions=1):\n    \"\"\"\n    The redesign that cannot exploit the model, built the same way every time.\n\n    Promoted once a third entry wanted it. The three constraints it applies are\n    the ones the unconstrained optimum was found to be abusing: twist that\n    oscillated to its bound, a tip chord that collapsed into a Reynolds regime\n    the section data does not cover, and span bought without paying for the spar\n    that carries it.\n\n    The Reynolds floor is the BASELINE's own tip value, not a number invented\n    for the purpose -- the design may not push the section anywhere the real\n    aircraft does not already operate -- and it is returned alongside the design\n    so an entry can assert the constraint actually bound.\n    \"\"\"\n    setup = baseline_setup() if setup is None else setup\n    floor = float(STATION_C[-1] * setup[\"speed\"] / KINEMATIC_VISCOSITY)\n    design = optimise(setup[\"limits\"], washout_monotone=True,\n                      reynolds_floor=floor, mass_model=True,\n                      subdivisions=subdivisions)\n    return {**design, \"reynolds_floor\": floor}\n\ndef method_verdicts(designs, setup):\n    \"\"\"\n    Each aerodynamic method's verdict on each design, against the baseline aircraft.\n\n    Promoted when a second entry compared the same two methods over a list of\n    designs. Bundling it also fixes the pairing: every verdict is measured\n    against the SAME baseline, computed by the same method that judges the\n    design, which is the only way percentages from different rows mean anything.\n\n    AeroBuildup carries the drag offset fitted to it; LiftingLine is read raw,\n    because that offset corrects an error LiftingLine does not make. So compare\n    changes between rows, never absolute heights.\n\n    Costs one alpha sweep per method per design, plus one per method for the\n    baseline.\n    \"\"\"\n    methods = [(\"AeroBuildup\", asb.AeroBuildup, setup[\"drag_offset\"], {}),\n               (\"LiftingLine\", asb.LiftingLine, 0.0, dict(spanwise_resolution=8))]\n    verdicts = {}\n    for label, analysis, offset, options in methods:\n        before = best_glide(analysis, AIRPLANE, setup[\"speed\"], offset, **options)\n        verdicts[label] = [\n            (best_glide(analysis,\n                        build_airplane(design[\"span\"], design[\"chords\"],\n                                       design[\"twists\"], design[\"fractions\"]),\n                        design[\"speed\"], offset, **options) / before - 1) * 100\n            for design in designs\n        ]\n    return verdicts\n\n```\n:::\n[Executed in 271.8 s · 101 aero solves]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-21-01-does-the-optimised-aircraft-change-with-more-control-points_files"
    ],
    "filters": [],
    "includes": {}
  }
}