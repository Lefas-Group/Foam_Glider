{
  "hash": "e27ba1c1f22c5574c59507790cb73000",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What wing bending loads does a +5.3 g limit manoeuvre produce?\"\n---\n\n\n::: {#3bce9177 .cell execution_count=2}\n``` {.python .cell-code code-summary=\"Show code\"}\nN_LIMIT = 5.3          # CS-22 limit manoeuvring load factor\nCL_MAX = 1.45          # assumed; see the callout below\nlimit_load = N_LIMIT * MASS * GRAVITY\n\n# The manoeuvre corner: the slowest speed at which the limit load factor can be\n# reached without stalling.\ndensity = asb.Atmosphere(altitude=0.0).density()\ncorner_speed = np.sqrt(2 * limit_load / (density * WING_AREA * CL_MAX))\n\nloads = spanwise_load(limit_load / 2, speed=corner_speed, alpha=8.0)\nstation_y = loads[\"station_y\"]\nstation_width = loads[\"width\"]\nload_per_metre = loads[\"load_per_metre\"]\n\n# Shear and bending moment by integrating inboard from the tip.\nshear = np.cumsum(loads[\"strip_force\"][::-1])[::-1]\nmoment = np.cumsum((shear * station_width)[::-1])[::-1]\n\nspar = wing_spar(station_y, load_per_metre)\n\nroot_moment = float(moment[0])\nroot_shear = float(shear[0])\ntip_deflection = float(spar.u[-1])\nshear_gap = abs(float(spar.shear_force[0]) - root_shear) / root_shear * 100\n\n# Two independent integrations of the same load. Agreement is the check that\n# earns the beam's deflection any credibility at all.\nassert shear_gap < 1.0, f\"beam and hand integration disagree by {shear_gap:.1f}%\"\n```\n:::\n\n\n::: {.hero}\n[40\\.1]{.hero-value}\n[kN·m of bending moment at the wing root]{.hero-label}\n:::\n\n**Answer.** At limit load the wing carries\n20\\.8 kN; the root sees\n[40\\.1]{.key} kN·m of bending moment and\n[10\\.40]{.key} kN of shear (@fig-loads). The\nlibrary's beam, integrated independently, agrees on root shear to\n[0\\.13]{.key}%. The loads are the robust part — they\nfollow from the lift distribution and the total load alone. Tip deflection of\n[1\\.15]{.key} m is not: it is only as good as the\ninvented spar.\n\n::: {.callout-tip}\n## Specified\nAsked of the user, 2026-09-17:\n\n1. **Load case**: limit manoeuvre, n = +5.3.\n:::\n\n::: {.callout-note}\n## Assumed\n1. **Maximum lift coefficient**: 1\\.45, flaps up.\n2. **Spar depth**: 0\\.95 of section thickness.\n3. **Wall thickness**: 11 mm root, tapering.\n4. **Elastic modulus**: 135 GPa carbon.\n5. **Tail load**: none; wing carries the whole limit load.\n:::\n\n::: {.callout-warning}\n## Tail carries nothing here\n\nA trimmed pull-up puts a download on the tailplane, so the real wing carries\nsomewhat more than this.\n:::\n\n::: {#cell-fig-loads .cell execution_count=3}\n``` {.python .cell-code}\nfig, (ax_load, ax_moment) = plt.subplots(2, 1, figsize=(7.0, 4.2), sharex=True)\n\nax_load.plot(station_y, load_per_metre)\nax_load.fill_between(station_y, 0, load_per_metre, alpha=0.15)\nax_load.set_ylabel(\"Airload [N/m]\")\nax_load.grid(alpha=0.3)\n\nax_moment.plot(station_y, moment / 1e3, color=\"C1\")\nax_moment.set_ylabel(\"Bending moment [kN·m]\")\nax_moment.set_xlabel(\"Distance from centreline [m]\")\nax_moment.grid(alpha=0.3)\nax_moment.set_xlim(0, SPAN / 2)\n\nplt.show()\n```\n\n::: {.cell-output .cell-output-display}\n![Spanwise airload and the resulting bending moment on one wing at n = +5.3. Load is normalized so the pair of wings carries exactly the limit load; the moment is integrated inboard from the tip.](2026-09-17-04-what-wing-bending-loads-does-a-5-3g-limit-manoeuvre-produce_files/figure-html/fig-loads-output-1.png){#fig-loads width=933 height=574}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef spanwise_load(total_lift, airplane=None, speed=30.0, alpha=8.0,\n                  spanwise=14, chordwise=6):\n    \"\"\"\n    Spanwise airload on one wing, from a linear method, scaled to a known total.\n\n    Promoted the moment a second entry wanted it. The panel bookkeeping below is\n    the part worth writing once: VLM emits panels ordered CHORDWISE-FASTEST, so a\n    reshape recovers the spanwise strips, and grouping on the y coordinate\n    instead does NOT work -- twist and dihedral move y by about a millimetre\n    across a chord, so every panel looks like its own strip. Both facts are\n    asserted rather than trusted.\n\n    A linear method on purpose. Structural load distributions are conventionally\n    built from one, because what the wing sees at high load factor is the\n    attached-flow (\"additional\") loading, which is exactly what an inviscid\n    method returns. AeroBuildup cannot do this at all: it exposes integrated\n    forces per component and no spanwise distribution.\n\n    VLM supplies the SHAPE only. The magnitude is set by `total_lift`, which the\n    caller knows exactly from the load factor, so the distribution is normalized\n    rather than trusted.\n\n    Args:\n        total_lift: lift carried by ONE wing, N.\n        airplane: defaults to the chapter's rigid geometry.\n        speed, alpha: the operating point the shape is taken at.\n\n    Returns dict of station_y, width, strip_force and load_per_metre.\n    \"\"\"\n    airplane = AIRPLANE if airplane is None else airplane\n    vlm = asb.VortexLatticeMethod(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(velocity=speed, alpha=alpha),\n        xyz_ref=XYZ_REF, spanwise_resolution=spanwise, chordwise_resolution=chordwise,\n    )\n    started = time.perf_counter()\n    vlm.run()\n    aero_cost[\"calls\"] += 1\n    aero_cost[\"seconds\"] += time.perf_counter() - started\n\n    on_wing = (vlm.vortex_centers[:, 0] < 4.0) & (vlm.vortex_centers[:, 1] > 0)\n    y_grid = vlm.vortex_centers[on_wing, 1].reshape(-1, chordwise)\n    width_grid = np.abs(vlm.right_vortex_vertices[on_wing, 1]\n                        - vlm.left_vortex_vertices[on_wing, 1]).reshape(-1, chordwise)\n    station_y = y_grid.mean(axis=1)\n    width = width_grid.mean(axis=1)\n\n    # Had the reshape mixed chordwise panels across strips, the strip stations\n    # would not come out monotonic -- a far better invariant than equal panel\n    # widths within a column, which twist and dihedral perturb by a few percent\n    # and which failed on a valid, strongly twisted geometry.\n    assert np.all(np.diff(station_y) > 0), \\\n        \"strip stations are not increasing; the reshape is not recovering strips\"\n    assert np.max(np.abs(width_grid - width[:, None]) / width[:, None]) < 0.05, \\\n        \"panel widths vary by over 5% within a strip\"\n    strip_force = vlm.forces_geometry[on_wing, 2].reshape(-1, chordwise).sum(axis=1)\n    strip_force = strip_force * total_lift / np.sum(strip_force)\n\n    return {\n        \"station_y\": station_y,\n        \"width\": width,\n        \"strip_force\": strip_force,\n        \"load_per_metre\": strip_force / width,\n    }\n\n```\n:::\n[Executed in 1.2 s · 1 aero solve]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-17-04-what-wing-bending-loads-does-a-5-3g-limit-manoeuvre-produce_files"
    ],
    "filters": [],
    "includes": {}
  }
}