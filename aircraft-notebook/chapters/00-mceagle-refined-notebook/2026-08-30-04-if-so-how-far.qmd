---
title: "If so, how far?"
---

{{< include _model.qmd >}}

```{python}
H, BUILT_M, SHIFT = 1.5, 0.016, 0.015

x_np = neutral_point()
x_cg_plan = x_np - 0.10 * c_w
plan = trim(0.01550, x_cg_plan)
built = trim(BUILT_M, x_cg_built := x_cg_plan + SHIFT)
a_stall, _ = stall(built["V"])

# The two claims the prose makes without quoting numbers.
assert built["alpha"] > a_stall                      # as built, past the stall
assert plan["alpha"] < stall(plan["V"])[0]           # as planned, below it
```

:::: {.hero-pair}
::: {.hero-item}
[`{python} f"{built['LD']*H:.1f}"` m]{.hero-value}
[as built]{.hero-label}
:::
::: {.hero-item}
[`{python} f"{plan['LD']*H:.1f}"` m]{.hero-value .hero-ref}
[as designed]{.hero-label}
:::
::::

::: {.callout-warning}
## Do not trust that number
The built glider trims *past* the stall, where the flow has separated and the
section data is unreliable. Read it as an upper bound on a nose-high mush.
Recovering the distance means moving the balance forward, not trimming for range.
:::

::: {.callout-tip}
## Specified
Measured on the built glider by the user, 2026-08-30:

1. **16 g**, balance **15 mm aft** of the plan's.
2. **Release height: 1.5 m**, as for the design.
:::

::: {.callout-note}
## Assumed
1. **Still air, and a glide already settled at trim.**
2. **Trim lies past the stall** — outside NeuralFoil's attached-flow regime.
:::

```{python}
#| echo: false
#| output: asis
show_source(trim)
```
