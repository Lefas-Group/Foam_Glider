---
title: "Fully optimized 5 mm glider"
order: 6
listing:
  id: entries
  contents: "20*.qmd"
  type: table
  fields: [title]
  # By filename, which starts with the date -- entries carry no `date:` of their
  # own, and within a chapter alphabetical order IS chronological order.
  sort: "filename"
  # A plain table. Chronological is the only order an append-only record has,
  # and a filter over three rows is furniture.
  filter-ui: false
  sort-ui: false
  field-display-names:
    title: "Entries"
---

{{< include _model.qmd >}}

```{python}
#| echo: false
#| output: asis
# GENERATED, from this chapter's _fork.yml (rule 35 keeps this cell here).
chapter_lineage("06-fully-optimized-5mm")
```

```{python}
#| echo: false
#| output: asis
# GENERATED, from this chapter's _inputs.yml and every _fork.yml that names it
# (rule 39 keeps this cell here). The callouts are NOT written by hand: an item
# a later chapter replaced moves into `## Superseded` with a link to it, which
# is impossible if the list is markdown, because a cell cannot annotate markup
# that is already on the page. Edit `_inputs.yml`.
chapter_inputs("06-fully-optimized-5mm")
```

::: {#entries}
:::

```{python}
#| echo: false
#| output: asis
import pathlib
for _f in ("_model.py", "_analysis.py"):
    _src = pathlib.Path(f"chapters/06-fully-optimized-5mm/{_f}").read_text()
    print(f'::: {{.callout-note collapse="true"}}')
    print(f"## `{_f}`\n")
    print(f"```python\n{_src}\n```")
    print(":::\n")
```
