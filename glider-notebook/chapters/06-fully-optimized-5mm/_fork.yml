# What this chapter changed, and from what. Read by rule 31, by the
# chapter index below it, and by the lineage diagram on the book index.
#
# Nothing else differs. An empty `diff` against the parent everywhere else
# is the positive check that says so.
parent: 05-fully-optimized
at: e2e92dc
# RECONSTRUCTED from filename dates against the fork commit's date,
# which is day-granular. Forks taken after this landed are exact:
# `create_chapter` counts the parent's entries at the moment it forks.
at_entry: 3
summary: "Returned to 5 mm foam."
changes:
  - airfoils restored to 5 mm stock: naca4405 for the wing, naca0005 for
    the tails
  - fuselage width restored to 0.010 m
# What this chapter REPLACED, as `<chapter>: <the item>`. The item is a
# numbered entry in that chapter's index callouts; naming it here is what
# lets its own page say it was later revisited, which an append-only record
# cannot say for itself. Rule 31 refuses a name that matches nothing.
supersedes:
  - 04-thinner-foam: Foam thickness
  - 04-thinner-foam: Foam density
  - 04-thinner-foam: Airfoil thickness
