# What this chapter changed, and from what. Read by rule 31, by the
# chapter index below it, and by the lineage diagram on the book index.
#
# Its _model.py was REWRITTEN rather than copied -- 84% similar to 04's, below
# rule 31's threshold, so nothing would have demanded this file. The design
# descends from 04 all the same: 04 is the 3 mm redesign, and this opens that
# same design to the optimiser. Without it the lineage diagram shows two
# unconnected trees and the notebook reads as two projects.
parent: 04-thinner-foam
at: f450884
# RECONSTRUCTED from filename dates against the fork commit's date,
# which is day-granular. Forks taken after this landed are exact:
# `create_chapter` counts the parent's entries at the moment it forks.
at_entry: 6
summary: "Opened the whole geometry to the optimiser."
changes:
  - every geometry variable is opened to the optimiser at once, where 04 fixed
    the tail and solved three wing variables
  - the model is rewritten around that rather than copied, so a diff against
    04 is not the review here -- the chapters differ everywhere
# What this chapter DEPARTED FROM, and what it put in place.
#   replaces:  - <chapter>/<their id>: <my id>
#   drops:     - <chapter>/<their id>: why it no longer holds
# It renders on THIS page, as "Changed from <chapter>" — the page that
# made the change. The chapter it departed from keeps its own items as
# they were: they are its premise and stay true under it.
drops:
  - 01-foam-glider/tail-size: the tail is a design variable here
