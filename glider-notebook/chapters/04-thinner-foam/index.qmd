---
title: "3 mm foam"
order: 4
listing:
  id: entries
  contents: "20*.qmd"
  type: table
  fields: [title]
  # By filename, which starts with the date -- entries carry no `date:` of their
  # own, and within a chapter alphabetical order IS chronological order.
  sort: "filename"
  # A plain table. Chronological is the only order an append-only record has,
  # and a filter over three rows is furniture.
  filter-ui: false
  sort-ui: false
  field-display-names:
    title: "Question"
---

{{< include _model.qmd >}}

```{python}
#| echo: false
#| output: asis
# GENERATED FROM THIS CHAPTER'S OWN _fork.yml (rule 35 keeps this block here).
# One source, three readers: rule 31 checks this file, the book index draws the
# lineage from it, and this prints what the fork actually changed.
#
# In a CALLOUT, like the Specified and Assumed it sits above. As a bare
# paragraph and a bare list it was the one un-styled region on the page, and it
# read as a mistake rather than as a section. One change renders as a sentence;
# a single-item bullet list is heavier than the sentence it replaced.
import pathlib
import re
_fk = pathlib.Path("chapters/04-thinner-foam/_fork.yml")
if _fk.exists():
    _t = _fk.read_text()
    _p = re.search(r"^parent:\s*(.+?)\s*$", _t, re.M)
    _items, _in = [], False
    for _l in _t.splitlines():
        if _l.startswith("#") or not _l.strip():
            continue
        if re.match(r"^\w+:", _l):
            _in = _l.startswith("changes:")
            continue
        if _in and _l.lstrip().startswith("-"):
            _items.append(_l.lstrip()[1:].strip())
        elif _in and _items and _l.startswith(" "):
            _items[-1] += " " + _l.strip()
    if _p:
        _par = pathlib.Path("chapters") / _p.group(1) / "index.qmd"
        _pt = re.search(r'^title:\s*"(.+)"\s*$', _par.read_text(), re.M) \
            if _par.exists() else None
        _name = _pt.group(1) if _pt else _p.group(1)
        print('::: {.callout-note appearance="simple"}')
        print(f"## Forked from [{_name}](../{_p.group(1)}/)\n")
        if len(_items) == 1:
            print(f"{_items[0]}\n")
        else:
            for _i in _items:
                print(f"* {_i}")
            print()
        print(":::\n")
```

A redesign of the glider for 3 mm foam instead of 5 mm foam. The foam area density is scaled proportionally, the airfoils are changed to 3% thickness (NACA 4403 and NACA 0003), and the doubled-foam fuselage width is reduced to 6 mm.

::: {.callout-tip}
## Specified

Asked of the user, 2026-09-15:

1. **Foam thickness**: 3 mm.

Inherited from earlier models:

2. **Span 300 mm**, fixed tip to tip.
3. **Objective**: minimum sink rate in trimmed glide.
4. **Fuselage**: 200 mm long, 15 mm high, doubled.
5. **Wing sweep**: constant quarter chord.
:::

::: {.callout-note}
## Assumed

1. **Foam density**: scaled linearly from 5 mm foam.
2. **Tail dimensions**: remain 100×30 mm and 50×30 mm.
3. **Airfoil thickness**: 3% to match foam.
:::

## Questions asked here

::: {#entries}
:::

## The model

```{python}
#| echo: false
#| output: asis
import pathlib
for _f in ("_model.py", "_analysis.py"):
    _src = pathlib.Path(f"chapters/04-thinner-foam/{_f}").read_text()
    print(f'::: {{.callout-note collapse="true"}}')
    print(f"## `{_f}`\n")
    print(f"```python\n{_src}\n```")
    print(":::\n")
```
