---
title: "First chapter"
---

{{< include _model.qmd >}}

<one sentence, then a bullet list: the aero method, the section, what is left out>

::: {.callout-tip}
## Specified

1. **<quantity>: <value>** — <why, if it fits>.
2. Solve budget `{python} f"{solve_budget():.0f}"` s, the notebook default.
:::

::: {.callout-note}
## Assumed

1. **<quantity>: <value>**, <the one-clause reason>.
:::

## The model

```{python}
#| echo: false
#| output: asis
import pathlib
for _f in ("_model.py", "_analysis.py"):
    _src = pathlib.Path(f"chapters/01-first-chapter/{_f}").read_text()
    print(f'::: {{.callout-note collapse="true"}}')
    print(f"## `{_f}`\n")
    print(f"```python\n{_src}\n```")
    print(":::\n")
```
