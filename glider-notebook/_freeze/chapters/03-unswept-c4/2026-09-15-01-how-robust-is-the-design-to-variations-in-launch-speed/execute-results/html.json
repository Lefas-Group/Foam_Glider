{
  "hash": "b69a10b1a70e4066092c836c49013fbe",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"How robust is the design to variations in launch speed?\"\n---\n\n\n::: {#cddae61e .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 60.0\nSOLVE_BUDGET = 60.0\n\nimport matplotlib.pyplot as plt\nimport aerosandbox.numpy as np\n\nres = optimize_glider_unswept_c4(verbose=False)\nsol = res['sol']\nglider = make_glider_unswept_c4(res['c_root'], res['taper'], res['cg_x'])\nmass = res['mass']\nalpha_trim = res['alpha']\n\nsim_2 = simulate_launch(glider, mass, alpha_trim, 2.2)\nsim_4 = simulate_launch(glider, mass, alpha_trim, 4.0)\nsim_6 = simulate_launch(glider, mass, alpha_trim, 6.0)\n\nmin_v2 = np.min(sim_2.y[2])\nmin_v4 = np.min(sim_4.y[2])\nmin_v6 = np.min(sim_6.y[2])\n```\n:::\n\n\n**Answer.** The glider is highly sensitive to launch speed. While it glides smoothly when launched near its trim speed, launching it faster causes it to pitch up sharply and stall due to the uncontrolled phugoid mode. At 4.0 m/s and 6.0 m/s, it drops to [0\\.67]{.key} m/s and [1\\.38]{.key} m/s, respectively, which is well below its stall speed.\n\n::: {.callout-tip}\n## New user specifications\nAsked of the user, 2026-09-15:\n\n1. **Launch speeds:** 2.2, 4.0, and 6.0 m/s.\n:::\n\n::: {.callout-note}\n## New assumptions\n1. **Simulation:** Point-mass with fixed alpha, lacking pitch inertia.\n:::\n\n::: {#cell-fig-launch-trajectories .cell execution_count=3}\n``` {.python .cell-code}\nplt.figure(figsize=(6, 4))\nplt.plot(sim_2.y[0], -sim_2.y[1], label=\"2.2 m/s\")\nplt.plot(sim_4.y[0], -sim_4.y[1], label=\"4.0 m/s\")\nplt.plot(sim_6.y[0], -sim_6.y[1], label=\"6.0 m/s\")\nplt.xlabel(\"Distance [m]\")\nplt.ylabel(\"Altitude [m]\")\nplt.legend(title=\"Launch speed\")\nplt.grid(True)\nplt.tight_layout()\n```\n\n::: {.cell-output .cell-output-display}\n![Flight paths simulated with a point-mass phugoid model for launches at 2.2, 4.0, and 6.0 m/s. Faster launches result in severe pitch-ups and stalls.](2026-09-15-01-how-robust-is-the-design-to-variations-in-launch-speed_files/figure-html/fig-launch-trajectories-output-1.png){#fig-launch-trajectories width=887 height=590}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_unswept_c4(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    # The vehicle function is already in scope because both files are exec'd into the notebook\n    airplane = make_glider_unswept_c4(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    fuse_mass = (0.2 * 0.015) * 2 * 0.1744 \n    mass = wings_area * 0.1744 + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sol.value(sink_rate),\n        \"c_root\": sol.value(c_root),\n        \"taper\": sol.value(taper),\n        \"cg_x\": sol.value(cg_x),\n        \"alpha\": sol.value(alpha),\n        \"mass\": sol.value(mass)\n    }\n\ndef simulate_launch(glider, mass, alpha_trim, v_launch, z0=-1.5, t_max=10.0):\n    V_grid = np.linspace(0.1, 15, 50)\n    L_grid = []\n    D_grid = []\n    \n    # Compute aero forces across speeds\n    for v in V_grid:\n        op = asb.OperatingPoint(velocity=v, alpha=alpha_trim)\n        aero = asb.AeroBuildup(airplane=glider, op_point=op).run()\n        L_grid.append(aero[\"L\"][0])\n        D_grid.append(aero[\"D\"][0])\n        \n    def _dynamics(t, state):\n        x, z, V_curr, gamma_curr = state\n        if V_curr < 0.1: return [0, 0, 0, 0]\n        L = np.interp(V_curr, V_grid, L_grid)\n        D = np.interp(V_curr, V_grid, D_grid)\n        x_dot = V_curr * np.cos(gamma_curr)\n        z_dot = -V_curr * np.sin(gamma_curr)\n        V_dot = -D/mass - 9.81 * np.sin(gamma_curr)\n        gamma_dot = (L/mass - 9.81 * np.cos(gamma_curr)) / V_curr\n        return [x_dot, z_dot, V_dot, gamma_dot]\n    \n    def _hit_ground(t, state):\n        return state[1]\n    _hit_ground.terminal = True\n    _hit_ground.direction = 1\n    \n    res = spi.solve_ivp(_dynamics, [0, t_max], [0, z0, v_launch, 0], events=_hit_ground, max_step=0.05)\n    return res\n\n```\n:::\n[Rendered in 12.3 s (limit 60 s) · 1 aero solve (budget 60 s each)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-15-01-how-robust-is-the-design-to-variations-in-launch-speed_files/figure-html"
    ],
    "filters": [],
    "includes": {}
  }
}