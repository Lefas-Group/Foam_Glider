{
  "hash": "1060998962e00d303c570a1ee9b737d3",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What are the dimensions of the fully optimized 3 mm glider?\"\n---\n\n\n::: {#32b1c34b .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 50.0   # s for this render, granted by the user\nSOLVE_BUDGET = 40.0     # s for any one solve\nPROBE_POOL = 130.0        # s granted for the probe\nPROBE_SPENT = 75.2      # s the probe actually used\n\nres = optimize_full_glider(verbose=False)\nsol = res['sol']\nairplane = res['airplane']\n\nc_root = float(sol.value(airplane.wings[0].xsecs[0].chord)) * 1000\nc_tip = float(sol.value(airplane.wings[0].xsecs[1].chord)) * 1000\nsweep = float(sol.value(res['sweep']))\ndihedral = float(sol.value(res['dihedral']))\nh_span = float(sol.value(res['h_span'])) * 1000\nh_chord = float(sol.value(res['h_chord'])) * 1000\nv_span = float(sol.value(airplane.wings[2].span())) * 1000\nv_chord = float(sol.value(airplane.wings[2].xsecs[0].chord)) * 1000\nboom_len = float(sol.value(res['boom_len'])) * 1000\nh_inc = float(sol.value(airplane.wings[1].xsecs[0].twist))\ncg_x = float(sol.value(airplane.xyz_ref[0])) * 1000\n\ndata = [\n    [\"Span (fixed)\", \"300.0 mm\", \"H-tail span\", f\"{h_span:.1f} mm\"],\n    [\"Root chord\", f\"{c_root:.1f} mm\", \"H-tail chord\", f\"{h_chord:.1f} mm\"],\n    [\"Tip chord\", f\"{c_tip:.1f} mm\", \"H-tail inc.\", f\"{h_inc:.1f}°\"],\n    [\"Wing sweep\", f\"{sweep:.1f}°\", \"V-tail span\", f\"{v_span:.1f} mm\"],\n    [\"Dihedral\", f\"{dihedral:.1f}°\", \"V-tail chord\", f\"{v_chord:.1f} mm\"],\n    [\"Tail boom\", f\"{boom_len:.1f} mm\", \"CG (from root LE)\", f\"{cg_x:.1f} mm\"]\n]\n\nmd = \"| Wing / Body | Dimension | Tail | Dimension |\\n|---|---|---|---|\\n\"\nfor row in data:\n    md += f\"| {row[0]} | {row[1]} | {row[2]} | {row[3]} |\\n\"\nmd += \": Table detailing the geometric dimensions and center of gravity for the fully optimized 3 mm foam glider. {#tbl-plans}\\n\"\n```\n:::\n\n\n**Answer.** The fully optimized plans feature a [\\-24\\.6]{.key}° forward sweep, strong taper down to an [8\\.4]{.key} mm tip, and a short [77\\.2]{.key} mm tail boom. Dimensions are extracted into (@tbl-plans).\n\n| Wing / Body | Dimension | Tail | Dimension |\n|---|---|---|---|\n| Span (fixed) | 300.0 mm | H-tail span | 63.4 mm |\n| Root chord | 83.9 mm | H-tail chord | 11.1 mm |\n| Tip chord | 8.4 mm | H-tail inc. | -7.7° |\n| Wing sweep | -24.6° | V-tail span | 42.1 mm |\n| Dihedral | 4.4° | V-tail chord | 10.0 mm |\n| Tail boom | 77.2 mm | CG (from root LE) | -5.4 mm |\n: Table detailing the geometric dimensions and center of gravity for the fully optimized 3 mm foam glider. {#tbl-plans}\n\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_full_glider(verbose=False, min_sweep=-45, init_sweep=0):\n    import aerosandbox as asb\n    import aerosandbox.numpy as np\n\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    sweep = opti.variable(init_guess=init_sweep, lower_bound=min_sweep, upper_bound=45)\n    dihedral = opti.variable(init_guess=5, lower_bound=0, upper_bound=20)\n\n    h_span = opti.variable(init_guess=0.1, lower_bound=0.02, upper_bound=0.3)\n    h_chord = opti.variable(init_guess=0.03, lower_bound=0.01, upper_bound=0.1)\n    v_span = opti.variable(init_guess=0.05, lower_bound=0.02, upper_bound=0.2)\n    v_chord = opti.variable(init_guess=0.03, lower_bound=0.01, upper_bound=0.1)\n    boom_len = opti.variable(init_guess=0.15, lower_bound=0.05, upper_bound=0.4)\n    h_inc = opti.variable(init_guess=0, lower_bound=-15, upper_bound=15)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n\n    airplane = make_full_glider(c_root, taper, sweep, dihedral, h_span, h_chord, v_span, v_chord, boom_len, h_inc, cg_x)\n\n    op1 = asb.OperatingPoint(velocity=V, alpha=alpha, beta=0)\n    op2 = asb.OperatingPoint(velocity=V, alpha=alpha+1, beta=0)\n    op3 = asb.OperatingPoint(velocity=V, alpha=alpha, beta=1)\n\n    aero1 = asb.AeroBuildup(airplane=airplane, op_point=op1).run()\n    aero2 = asb.AeroBuildup(airplane=airplane, op_point=op2).run()\n    aero3 = asb.AeroBuildup(airplane=airplane, op_point=op3).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    area_density = 0.10464\n    fuse_mass = ((boom_len + 0.05) * 0.015) * 2 * area_density\n    mass = wings_area * area_density + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero1[\"L\"] == weight * np.cosd(gamma),\n        aero1[\"D\"] == weight * np.sind(-gamma),\n        aero1[\"Cm\"] == 0\n    ])\n\n    dCm_dalpha = aero2['Cm'] - aero1['Cm']\n    dCL_dalpha = aero2['CL'] - aero1['CL']\n    SM = -dCm_dalpha / dCL_dalpha\n\n    dCn_dbeta = aero3['Cn'] - aero1['Cn']\n    dCl_dbeta = aero3['Cl'] - aero1['Cl']\n\n    opti.subject_to([\n        SM >= 0.10,\n        dCn_dbeta >= 0.0005,\n        dCl_dbeta <= -0.0005\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"opti\": opti,\n        \"sol\": sol,\n        \"airplane\": airplane,\n        \"mass\": mass,\n        \"sink_rate\": sink_rate,\n        \"sweep\": sweep,\n        \"dihedral\": dihedral,\n        \"h_span\": h_span,\n        \"h_chord\": h_chord,\n        \"boom_len\": boom_len\n    }\n\n```\n:::\n[Rendered in 15.6 s (limit 50 s) · 1 aero solve (budget 40 s each) · explored in 75 s (limit 130 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-21-01-what-are-the-dimensions-of-the-fully-optimized-3-mm-glider_files"
    ],
    "filters": [],
    "includes": {}
  }
}