{
  "hash": "19c80c63a05140f6395727654b54bdde",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What does the 3 mm glider look like?\"\n---\n\n\n::: {#273a72f7 .cell execution_count=2}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\n\nENTRY_CEILING = 60.0\nSOLVE_BUDGET = 15.0\nPROBE_POOL = 120.0\nPROBE_SPENT = 25.9\n\nres = optimize_glider_3mm(verbose=False)\nglider = make_glider_3mm(res['c_root'], res['taper'], res['cg_x'])\n```\n:::\n\n\n**Answer.** The optimized 3 mm glider features a highly tapered wing with an unswept quarter chord, mounted to the doubled-foam fuselage ahead of the fixed tail surfaces.\n\n::: {#cell-fig-three-view .cell execution_count=3}\n``` {.python .cell-code}\nfig = plt.figure(figsize=(8, 6))\naxs = fig.subplots(2, 2, subplot_kw={'projection': '3d'})\nglider.draw_three_view(axs=axs, show=False)\n```\n\n::: {.cell-output .cell-output-display}\n![Three-view drawing of the optimized 3 mm foam glider, showing the high-taper wing planform and side profile with the doubled-foam fuselage.](2026-09-21-01-what-does-the-3-mm-glider-look-like_files/figure-html/fig-three-view-output-1.png){#fig-three-view width=1207 height=1074}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_3mm(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_3mm(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    # area density of 3mm foam = 174.4 * (3/5) = 104.64 g/m^2\n    area_density = 0.10464\n    fuse_mass = (0.2 * 0.015) * 2 * area_density\n    mass = wings_area * area_density + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sol.value(sink_rate),\n        \"c_root\": sol.value(c_root),\n        \"taper\": sol.value(taper),\n        \"cg_x\": sol.value(cg_x),\n        \"alpha\": sol.value(alpha),\n        \"mass\": sol.value(mass)\n    }\n\n```\n:::\n[Rendered in 3.8 s (limit 60 s) · 1 aero solve (budget 15 s each) · explored in 26 s (limit 120 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-21-01-what-does-the-3-mm-glider-look-like_files"
    ],
    "filters": [],
    "includes": {}
  }
}