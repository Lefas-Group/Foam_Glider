{
  "hash": "3d2fc2d51b143568c0c435c5d9f94459",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Will the 3 mm wing survive 1g flight loads?\"\n---\n\n\n::: {#7b66aa8a .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 20.0   # s for this render, granted by the user\nSOLVE_BUDGET = 2.0     # s for any one solve\n\nimport aerosandbox.numpy as np\n\nres = optimize_glider_3mm(verbose=False)\nc_root = res['c_root']\ntaper = res['taper']\nmass = res['mass']\n\nweight = mass * 9.81\nL_half = weight / 2\nb_half = 0.15\n\n# Compute root bending moment from strip theory\ny = np.linspace(0, b_half, 100)\nc = c_root * (1 - (1 - taper) * (y / b_half))\nq = c / np.trapezoid(c, y) * L_half\nM_root = np.trapezoid(q * y, y)\n\n# Solid rectangular section modulus\nt = 0.003\nZ_root = (c_root * t**2) / 6\nmax_stress = M_root / Z_root\n\nyield_stress = 300_000  # Pa\nsafety_factor = yield_stress / max_stress\n```\n:::\n\n\n::: {.hero}\n[4\\.7 kPa]{.hero-value}\n[Maximum root bending stress]{.hero-label}\n:::\n\n**Answer.** The 3 mm wing easily survives the 1g flight loads of an ideal launch. The root bending stress is about [4\\.7]{.key} kPa, providing a safety factor over [64]{.key} against the foam's assumed yield strength.\n\n::: {.callout-note}\n## New assumptions\n1. **Foam yield stress: 300 kPa**, determines safety factor.\n:::\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_3mm(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_3mm(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    # area density of 3mm foam = 174.4 * (3/5) = 104.64 g/m^2\n    area_density = 0.10464\n    fuse_mass = (0.2 * 0.015) * 2 * area_density\n    mass = wings_area * area_density + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sol.value(sink_rate),\n        \"c_root\": sol.value(c_root),\n        \"taper\": sol.value(taper),\n        \"cg_x\": sol.value(cg_x),\n        \"alpha\": sol.value(alpha),\n        \"mass\": sol.value(mass)\n    }\n\n```\n:::\n[Rendered in 2.7 s (limit 20 s) · 1 aero solve (budget 2 s each)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-15-03-will-the-3-mm-wing-survive-1g-flight-loads_files"
    ],
    "filters": [],
    "includes": {}
  }
}