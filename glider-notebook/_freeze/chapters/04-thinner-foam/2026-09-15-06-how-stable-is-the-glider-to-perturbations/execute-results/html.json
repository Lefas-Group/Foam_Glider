{
  "hash": "97717bcd4f3a83fa4e9ab6b34be6d041",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"How stable is the glider to perturbations?\"\n---\n\n\n::: {#ce6ba1d6 .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 20.0\nSOLVE_BUDGET = 15.0\nPROBE_POOL = 120.0\nPROBE_SPENT = 55.1\n\nimport scipy.optimize as opt_sci\nimport aerosandbox as asb\n\nopt = optimize_glider_3mm(verbose=False)\nc_root = float(opt[\"c_root\"])\ntaper = float(opt[\"taper\"])\ncg_x = float(opt[\"cg_x\"])\nalpha = float(opt[\"alpha\"])\nmass = float(opt[\"mass\"])\nweight = mass * 9.81\n\nairplane = make_glider_3mm(c_root, taper, cg_x)\n\ndef get_err(V_test):\n    op = asb.OperatingPoint(velocity=float(V_test), alpha=alpha)\n    aero = asb.AeroBuildup(airplane, op).run()\n    return float(aero[\"L\"][0]) - weight\n\nres = opt_sci.root_scalar(get_err, bracket=[0.5, 20.0])\ntrim_v = res.root\n\nop = asb.OperatingPoint(velocity=trim_v, alpha=alpha)\nderivs = asb.AeroBuildup(airplane, op).run_with_stability_derivatives()\n\ncma = float(derivs['Cma'][0])\ncnb = float(derivs['Cnb'][0])\nclb = float(derivs['Clb'][0])\n```\n:::\n\n\n**Answer.** The optimized glider is statically unstable in pitch ($C_{m_{\\alpha}}$ = \\+0\\.39) and slightly unstable in roll ($C_{l_{\\beta}}$ = \\+0\\.003), but remains stable in yaw ($C_{n_{\\beta}}$ = \\+0\\.064). The optimizer placed the center of gravity behind the neutral point because static stability was never constrained during the trimmed glide optimization.\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_3mm(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_3mm(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    # area density of 3mm foam = 174.4 * (3/5) = 104.64 g/m^2\n    area_density = 0.10464\n    fuse_mass = (0.2 * 0.015) * 2 * area_density\n    mass = wings_area * area_density + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sol.value(sink_rate),\n        \"c_root\": sol.value(c_root),\n        \"taper\": sol.value(taper),\n        \"cg_x\": sol.value(cg_x),\n        \"alpha\": sol.value(alpha),\n        \"mass\": sol.value(mass)\n    }\n\n```\n:::\n[Rendered in 3.5 s (limit 20 s) · 1 aero solve (budget 15 s each) · explored in 55 s (limit 120 s)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-15-06-how-stable-is-the-glider-to-perturbations_files"
    ],
    "filters": [],
    "includes": {}
  }
}