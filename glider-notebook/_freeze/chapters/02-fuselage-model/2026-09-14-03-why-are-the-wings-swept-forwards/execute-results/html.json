{
  "hash": "a922c367a1dbedfb72df7fc19208891b",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"Why are the wings swept forwards?\"\n---\n\n\n::: {#4c561e74 .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 100.0   # s for this render, granted by the user\nSOLVE_BUDGET = 60.0     # s for any one solve\n\nres = optimize_glider_with_fuse(verbose=False)\nsol = res['sol']\nglider = make_glider_with_fuse(\n    sol.value(res['c_root']), \n    sol.value(res['taper']), \n    sol.value(res['cg_x'])\n)\nwing = glider.wings[0]\n\nle_sweep = wing.mean_sweep_angle(0)\nc4_sweep = wing.mean_sweep_angle(0.25)\nte_sweep = wing.mean_sweep_angle(1.0)\n```\n:::\n\n\n::: {.hero}\n[12\\.7°]{.hero-value}\n[quarter-chord forward sweep]{.hero-label}\n:::\n\n**Answer.** The leading edge is defined as completely straight (0° sweep). The optimizer chose a taper ratio of roughly 0\\.10 to reduce induced drag. Because the chord shrinks while the leading edge stays fixed, the quarter-chord line—which defines aerodynamic sweep—is pulled forward at the tips, creating 12\\.7° of forward sweep. The trailing edge is swept forward even more (42\\.0°).\n\n::: {.callout-tip}\n## Specified\nAsked of the user, 2026-09-14:\n\n1. **Render budget:** 100 s.\n2. **Solve budget:** 60 s.\n:::\n\n::: {#cell-fig-sweep .cell execution_count=3}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\n\nfig, ax = plt.subplots(figsize=(6, 4))\nc_root_val = sol.value(res['c_root']) * 1000\ntaper_val = sol.value(res['taper'])\nc_tip_val = c_root_val * taper_val\nb_half = 150 # mm\n\n# Right wing half\nax.plot([0, b_half], [0, 0], 'k-', label=\"Leading edge (0°)\")\nax.plot([0, b_half], [-c_root_val, -c_tip_val], 'k-', label=\"Trailing edge\")\nax.plot([0, b_half], [-c_root_val * 0.25, -c_tip_val * 0.25], 'r--', label=\"Quarter-chord\")\nax.plot([0, 0], [0, -c_root_val], 'k-')\nax.plot([b_half, b_half], [0, -c_tip_val], 'k-')\n\n# Left wing half\nax.plot([0, -b_half], [0, 0], 'k-')\nax.plot([0, -b_half], [-c_root_val, -c_tip_val], 'k-')\nax.plot([0, -b_half], [-c_root_val * 0.25, -c_tip_val * 0.25], 'r--')\nax.plot([-b_half, -b_half], [0, -c_tip_val], 'k-')\n\nax.set_aspect('equal')\nax.set_xlabel(\"Spanwise position (mm)\")\nax.set_ylabel(\"Chordwise position (mm) [Negative aft]\")\nax.legend(loc='lower center')\n```\n\n::: {.cell-output .cell-output-display}\n![Top-down view of the wing planform, showing the straight leading edge and the resulting forward sweep of the quarter-chord and trailing edge lines.](2026-09-14-03-why-are-the-wings-swept-forwards_files/figure-html/fig-sweep-output-1.png){#fig-sweep width=815 height=451}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_with_fuse(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_with_fuse(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    fuse_mass = (0.2 * 0.015) * 2 * 0.1744 \n    mass = wings_area * 0.1744 + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sink_rate,\n        \"mass\": mass,\n        \"c_root\": c_root,\n        \"taper\": taper,\n        \"cg_x\": cg_x,\n        \"V\": V,\n        \"gamma\": gamma,\n        \"alpha\": alpha\n    }\n\n```\n:::\n[Executed in 2.1 s]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-14-03-why-are-the-wings-swept-forwards_files"
    ],
    "filters": [],
    "includes": {}
  }
}