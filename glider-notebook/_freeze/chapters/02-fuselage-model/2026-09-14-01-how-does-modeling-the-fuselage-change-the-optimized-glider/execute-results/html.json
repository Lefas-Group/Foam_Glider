{
  "hash": "6f64814b2ca5ca6208050664974e9fae",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"How does it change if we model the fuselage and its mass?\"\n---\n\n\n::: {#8399d7f2 .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 200.0   # s for this render (rule 28)\nSOLVE_BUDGET = 60.0     # s for any one solve\nres = optimize_glider_with_fuse()\nsol = res[\"sol\"]\n\nnew_sink = sol.value(res[\"sink_rate\"])\nnew_mass = sol.value(res[\"mass\"])\nnew_c_root = sol.value(res[\"c_root\"])\n\nold_sink = 0.36\nold_mass = 0.00356\nold_c_root = 0.0965\n\nc_root_bound = 0.15\n```\n:::\n\n\n::: {.hero-pair}\n[0\\.36]{.hero-value}\n[m/s sink rate]{.hero-label}\n\n[0\\.40]{.hero-value}\n[with fuselage]{.hero-label}\n:::\n\n**Answer.** Adding the fuselage increases the glider's mass, which drives the wing root chord to its maximum bound to carry the extra weight. This keeps the sink rate penalty to a minimum.\n\n::: {.callout-tip}\n## New user specifications\nAsked of the user, 2026-09-14:\n\n1. **Fuselage: 5 mm foam**, doubled up.\n2. **Dimensions: 15 mm high, 200 mm long**.\n:::\n\n::: {.callout-note}\n## New assumptions\n1. **Fuselage width: 10 mm**, doubled 5 mm foam.\n:::\n\n| Metric | No Fuselage | With Fuselage |\n|---|---|---|\n| Mass (g) | 3.6 | 6.1 |\n| Root Chord (mm) | 96 | 150 |\n| Sink Rate (m/s) | 0.36 | 0.40 |\n\n: Comparison of optimized glider parameters.\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_with_fuse(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_with_fuse(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    fuse_mass = (0.2 * 0.015) * 2 * 0.1744 \n    mass = wings_area * 0.1744 + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sink_rate,\n        \"mass\": mass,\n        \"c_root\": c_root,\n        \"taper\": taper,\n        \"cg_x\": cg_x,\n        \"V\": V,\n        \"gamma\": gamma,\n        \"alpha\": alpha\n    }\n\n```\n:::\n[Rendered in 2.6 s (limit 200 s) · 1 aero solve (budget 60 s each)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-14-01-how-does-modeling-the-fuselage-change-the-optimized-glider_files"
    ],
    "filters": [],
    "includes": {}
  }
}