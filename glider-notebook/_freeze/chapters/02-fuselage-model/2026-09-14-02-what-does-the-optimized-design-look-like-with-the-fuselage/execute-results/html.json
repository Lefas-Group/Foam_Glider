{
  "hash": "13360e9f001f78a8cbeaab9720f0bc0e",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What does the optimized design look like with the fuselage?\"\n---\n\n\n::: {#a133a650 .cell execution_count=2}\n``` {.python .cell-code}\nENTRY_CEILING = 200.0   # s for this render (rule 28)\nSOLVE_BUDGET = 60.0     # s for any one solve\nimport matplotlib.pyplot as plt\nimport aerosandbox.numpy as np\n\nres = optimize_glider_with_fuse(verbose=False)\nsol = res[\"sol\"]\nc_root_opt = sol.value(res[\"c_root\"])\ntaper_opt = sol.value(res[\"taper\"])\ncg_x_opt = sol.value(res[\"cg_x\"])\n\nairplane_opt = make_glider_with_fuse(c_root_opt, taper_opt, cg_x_opt)\n```\n:::\n\n\n**Answer.** Adding the fuselage and its mass drives the wing root chord to its maximum bound of [0\\.15]{.key} m, resulting in a noticeably larger wing area compared to the bare-wing design (@fig-three-view-fuselage).\n\n::: {#cell-fig-three-view-fuselage .cell execution_count=3}\n``` {.python .cell-code}\nfig = plt.figure(figsize=(7, 6))\naxs = np.array([\n    [fig.add_subplot(2, 2, 1, projection='3d'), fig.add_subplot(2, 2, 2, projection='3d')],\n    [fig.add_subplot(2, 2, 3, projection='3d'), fig.add_subplot(2, 2, 4, projection='3d')]\n])\nairplane_opt.draw_three_view(axs=axs, show=False)\n```\n\n::: {.cell-output .cell-output-display}\n![Three-view drawing of the optimized foam glider with fuselage.](2026-09-14-02-what-does-the-optimized-design-look-like-with-the-fuselage_files/figure-html/fig-three-view-fuselage-output-1.png){#fig-three-view-fuselage width=1141 height=1074}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider_with_fuse(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider_with_fuse(c_root, taper, cg_x)\n    aero = asb.AeroBuildup(airplane=airplane, op_point=asb.OperatingPoint(velocity=V, alpha=alpha)).run()\n\n    wings_area = airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()\n    fuse_mass = (0.2 * 0.015) * 2 * 0.1744 \n    mass = wings_area * 0.1744 + fuse_mass\n    weight = mass * 9.81\n\n    opti.subject_to([\n        aero[\"L\"] == weight * np.cosd(gamma),\n        aero[\"D\"] == weight * np.sind(-gamma),\n        aero[\"Cm\"] == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sink_rate,\n        \"mass\": mass,\n        \"c_root\": c_root,\n        \"taper\": taper,\n        \"cg_x\": cg_x,\n        \"V\": V,\n        \"gamma\": gamma,\n        \"alpha\": alpha\n    }\n\n```\n:::\n[Rendered in 5.0 s (limit 200 s) · 1 aero solve (budget 60 s each)]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-14-02-what-does-the-optimized-design-look-like-with-the-fuselage_files"
    ],
    "filters": [],
    "includes": {}
  }
}