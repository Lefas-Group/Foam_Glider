{
  "hash": "1abf2063380ab64360a16921825471bc",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: \"What does the optimized design look like?\"\n---\n\n\n::: {#21be7396 .cell execution_count=2}\n``` {.python .cell-code}\nimport matplotlib.pyplot as plt\nimport aerosandbox.numpy as np\n\nres = optimize_glider(verbose=False)\nsol = res[\"sol\"]\nc_root_opt = sol.value(res[\"vars\"][\"c_root\"])\ntaper_opt = sol.value(res[\"vars\"][\"taper\"])\ncg_x_opt = sol.value(res[\"vars\"][\"cg_x\"])\n\nairplane_opt = make_glider(c_root_opt, taper_opt, cg_x_opt)\n```\n:::\n\n\n**Answer.** The optimized planform has a [0\\.10]{.key} m root chord and tapers sharply to its lower bound of [0\\.1]{.key}. Span, sections, and tail match the chapter constraints (@fig-planform).\n\n::: {#fig-planform .cell execution_count=3}\n``` {.python .cell-code}\nfig = plt.figure(figsize=(7, 6))\naxs = np.array([\n    [fig.add_subplot(2, 2, 1, projection='3d'), fig.add_subplot(2, 2, 2, projection='3d')],\n    [fig.add_subplot(2, 2, 3, projection='3d'), fig.add_subplot(2, 2, 4, projection='3d')]\n])\nairplane_opt.draw_three_view(axs=axs, show=False)\n```\n\n::: {#fig-planform-1 .cell-output .cell-output-display execution_count=3}\n```\narray([[<Axes3D: zlabel='$z_g$ [m]'>, <Axes3D: >],\n       [<Axes3D: xlabel='$x_g$ [m]', ylabel='$y_g$ [m]'>, <Axes3D: >]],\n      dtype=object)\n```\n\nThree-view drawing of the optimized foam glider planform.\n:::\n\n::: {.cell-output .cell-output-display}\n![](2026-09-14-01-what-does-the-optimized-design-look-like_files/figure-html/fig-planform-output-2.png){#fig-planform-2 width=1141 height=1074}\n:::\n:::\n\n\n::: {.callout-note collapse=\"true\"}\n## The method, as called\n\n```python\ndef optimize_glider(verbose=False):\n    opti = asb.Opti()\n    c_root = opti.variable(init_guess=0.08, lower_bound=0.01, upper_bound=0.15)\n    taper = opti.variable(init_guess=0.5, lower_bound=0.1, upper_bound=1.0)\n    alpha = opti.variable(init_guess=5, lower_bound=-5, upper_bound=15)\n    V = opti.variable(init_guess=3, lower_bound=1, upper_bound=20)\n    gamma = opti.variable(init_guess=-5, lower_bound=-45, upper_bound=45)\n    cg_x = opti.variable(init_guess=0.03, lower_bound=-0.05, upper_bound=0.15)\n\n    airplane = make_glider(c_root, taper, cg_x)\n\n    aero = asb.AeroBuildup(\n        airplane=airplane,\n        op_point=asb.OperatingPoint(\n            velocity=V,\n            alpha=alpha\n        )\n    ).run()\n\n    mass = (airplane.wings[0].area() + airplane.wings[1].area() + airplane.wings[2].area()) * 0.1744\n    weight = mass * 9.81\n\n    L = aero[\"L\"]\n    D = aero[\"D\"]\n    Cm = aero[\"Cm\"]\n\n    opti.subject_to([\n        L == weight * np.cosd(gamma),\n        D == weight * np.sind(-gamma),\n        Cm == 0\n    ])\n\n    sink_rate = V * np.sind(-gamma)\n    opti.minimize(sink_rate)\n    sol = opti.solve(verbose=verbose)\n    \n    return {\n        \"sol\": sol,\n        \"sink_rate\": sink_rate,\n        \"L\": L,\n        \"D\": D,\n        \"vars\": {\n            \"c_root\": c_root,\n            \"taper\": taper,\n            \"alpha\": alpha,\n            \"V\": V,\n            \"gamma\": gamma,\n            \"cg_x\": cg_x,\n        },\n        \"bounds\": {\n            \"c_root\": (0.01, 0.15),\n            \"taper\": (0.1, 1.0),\n            \"alpha\": (-5, 15),\n            \"V\": (1, 20),\n            \"gamma\": (-45, 45),\n            \"cg_x\": (-0.05, 0.15),\n        }\n    }\n\n```\n:::\n[Executed in 3.9 s]{.runtime}\n\n\n",
    "supporting": [
      "2026-09-14-01-what-does-the-optimized-design-look-like_files"
    ],
    "filters": [],
    "includes": {}
  }
}